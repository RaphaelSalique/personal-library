{% extends 'base.html.twig' %}

{% block title %}Supprimer l'éditeur {{ editor.name }}{% endblock %}

{% block body %}
    <div class="columns">
        <div class="column is-4">
            <div class="box">
                <h1>Supprimer l'éditeur {{ editor.name }}</h1>
                <br>
                {{ form(form) }}
            </div>
        </div>
    </div>

{% endblock %}
